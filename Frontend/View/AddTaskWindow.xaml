<Window x:Class="IntroSE.Kanban.Frontend.View.AddTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IntroSE.Kanban.Frontend.View"
        mc:Ignorable="d"
        Title="New Task"
        Height="300"
        Width="500"
        Background="#F4F4F4"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

  <Grid>
    <Border Margin="10"
            Background="White"
            CornerRadius="10"
            Padding="20"
            BorderBrush="#DDDDDD"
            BorderThickness="1">
      <StackPanel Margin="0,0,-21,-26">

        <Label Content="Create New Task"
               FontSize="20"
               FontWeight="Bold"
               Foreground="#2C3E50"
               HorizontalAlignment="Center"
               Margin="0,0,0,20"/>

        <Grid Margin="0,0,0,20">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
          </Grid.RowDefinitions>

          <Label Content="Title:" Grid.Row="0" Grid.Column="0"
                 VerticalAlignment="Center" Margin="0,0,10,5"/>
          <TextBox Grid.Row="0" Grid.Column="1" Height="25" Width="350"
                   Text="{Binding Title, Mode=OneWayToSource}" />

          <Label Content="Description:" Grid.Row="1" Grid.Column="0"
                 VerticalAlignment="Top" Margin="0,0,10,5"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Height="60" Width="350"
                   TextWrapping="Wrap" AcceptsReturn="True"
                   Text="{Binding Description, Mode=OneWayToSource}" />

          <Label Content="Due Date:" Grid.Row="2" Grid.Column="0"
                 VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <DatePicker Grid.Row="2" Grid.Column="1" Height="25" Width="350"
                      SelectedDate="{Binding DueDate, Mode=OneWayToSource}"/>
        </Grid>

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,10">
          <Button Content="OK"
                  Width="100"
                  Margin="5,0"
                  Background="#7F8CAA"
                  Foreground="White"
                  Click="Ok_Click"/>
          <Button Content="Cancel"
                  Width="100"
                  Margin="5,0"
                  Background="#333446"
                  Foreground="White"
                  Click="Cancel_Click"/>
        </StackPanel>

        <Label Foreground="Red"
               Content="{Binding ErrorMessage, Mode=OneWay}"
               HorizontalAlignment="Center"/>

      </StackPanel>
    </Border>
  </Grid>
</Window>
